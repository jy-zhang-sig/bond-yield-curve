# 利率曲线分析网站 — 代码与功能解析及优化评价

> 仓库：`harry-li-sf/Bond-yield-curve`（GitHub Pages 纯静态站）
> 线上：https://harry-li-sf.github.io/Bond-yield-curve/
> 评审日期：2026-08-06 ｜ 仅评审，未做任何改动

---

## 一、项目总览

这是一个面向寿险精算的**中债收益率曲线监测与分析平台**，纯前端静态站点 + GitHub Actions 自动数据管道。覆盖 9 种债券 × 2 种收益率口径 = 18 组曲线，并衍生出溢价评估利率、折现率生成、预定利率研究值三大业务板块。

**技术栈**：原生 HTML/CSS/JS（无框架、无构建工具）+ ECharts（图表）+ SheetJS/XLSX（Excel 导入导出）+ GitHub Actions（Python 数据抓取）。

---

## 二、文件结构

| 文件 | 行数 | 作用 |
|---|---|---|
| `index.html` | 5323 | 主程序，内联全部 CSS（~247 行）+ HTML 骨架 + JS（~4547 行） |
| `ci_update.py` | 1046 | Actions 数据抓取脚本，生成 18 个曲线 JSON + 派生文件 |
| `methodology.html` | 129 | 第三板块公式与数据源说明页 |
| `preset_model_data.js` | 1(压缩) | 预定利率模型数据（Actions 每日生成） |
| `data*.json` × 18 | — | 9 债券 × 2 口径的基础曲线数据 |
| `summary.json` | — | 首页总览卡片摘要 |
| `life_discount.json` | — | 寿险折现率派生数据（由基础曲线计算） |
| `data_makeup_weekend_audit.json` | — | 周末补班候选日核验状态 |
| `.github/workflows/update-data.yml` | 125 | 每工作日 5 次 cron 自动抓取+部署 |
| `assets/` | — | echarts.min.js、xlsx.full.min.js 本地副本 |
| `tests/` | — | CI 脚本与前端自动化测试 |

---

## 三、三大业务板块（功能点）

### 板块一：基础评估曲线（默认入口）

- **总览仪表盘**：2×2 卡片网格，18 组曲线以卡片形式呈现，10Y 主力期限大字 + 1Y/5Y/20Y/30Y 辅助格，点击进入详情。
- **债券数据汇总**：最新日期指标行，支持即期 / 750日 / 20日 / 30日 / 60日平均口径切换。
- **即期利率曲线图**：多时点对比（ECharts），可选查看日期。
- **关键期限利率表**：7 个关键期限（1/5/10/15/20/30/50Y），对比上年度末/上月末/上日，列含 bp 变动；支持"平均X"分段。
- **历史关键时点对比**：年末/月末热力图（按列独立着色），A/B 两日期差值表。
- **移动平均利率预测**：平推法预测 1/2/3/10 年（250/500/750/2500 日），支持时间序列 / 曲线快照两种视图 + 末月播放动画。
- **季度末移动平均预测**：关键期限 × 动态周期表，含 1-10Y/1-20Y/21-40Y 加权列，可导出 XLSX。

### 板块二：溢价评估利率

- **溢价监测**（可折叠）：
  - 前 20 年税收、流动性溢价（标的 MA − 国债 MA，旧/新准则）
  - 前 20 年逆周期溢价（旧准则：(国债2500MA − 国债750MA) × 25%）
  - 40 年后溢价（旧/新准则，基准期限 10/15/20Y）
  - 每项支持 3 个观察时点 + 评估时点 + 预测时点（平推），MA 周期可选 250/750/2500。
- **折现率生成**（可折叠，旧/新准则左右并排）：
  - 参数面板（评估时点、对比时点、前20年/长期溢价 bp、终极利率）
  - 基础曲线 / 综合溢价 / 即期折现率 / 远期折现率 四指标切换
  - 图表 + 结果表，可导出 XLSX
  - 旧准则：国债 750MA→4.5% 终极；新准则：国债即期→4.5% 终极，20-40 年过渡
- **移动平均分析**：时间序列图 + 期限曲线图，基础/对比曲线叠加，MA 周期可勾选+自定义添加，表格可导出。
- （隐藏）寿险合同负债评估折现率曲线卡 — 当前 `display:none`，功能保留但未启用。

### 板块三：预定利率研究值

- 读取本地 `preset_model_data.js`（由 Actions 从公开模型源转换）。
- 政策性金融债代表口径切换：三者均值 / 国开债 / 农发行债 / 进出口行债。
- 指标卡：模型预测值 / 协会实际值 / 普通型预定利率上限 / 上限−模型差。
- 公式：`模型预测值 = MIN(负债端利率锚, 资产端回报)`；负债端锚 = MA6(LPR5Y + 六大行5年定存均值)/2；资产端 = MIN(MA250, MA750)(10Y 政金债)。
- 触发监测：25BP 触发线进度条 + 监测状态。
- 历史回测：协会实际值 vs 模型值误差（BP）。
- 协会实际研究值对照表 + 历史走势图（9 条序列，含 dataZoom）。

---

## 四、数据管道（CI）

- **触发**：每工作日北京时间 18:13/19:37/20:23/21:47/22:19 共 5 次 cron + push + 手动。
- **流程**：`actions/checkout` → 设上海时区 → 跑 `tests/` 单测 → `ci_update.py`（push 时仅 `--derived-only` 重新派生）→ 校验文件大小 → git 提交推送 → 部署 GitHub Pages。
- **数据源**：中债登 `POST https://yield.chinabond.com.cn/cbweb-mn/yc/searchYc`，`qxll=1` 即期 / `qxll=0` 到期，只保留整数年限点。
- **特殊处理**：地方政府债无官方即期，用到期收益率 bootstrap 推导；周末补班候选日对 18 组曲线逐一核验，已有日期不重抓。
- **派生**：`life_discount.json` 只存紧凑数据（750MA、20年内溢价曲线、监测所需 MA 行），前端即时计算综合溢价/即期/远期折现率。
- **keepalive**：无数据变更时若距上次提交 >23h，生成空提交防 Pages 休眠。

---

## 五、前端核心算法

| 算法 | 实现要点 |
|---|---|
| N 日移动平均 | 前缀和（prefix sum）加速：`buildMAPrefixSums()` 一次性 O(N×期限数) 构建，查询 O(1)；仅累计"全期限有效"的行；dataStore 变化时自动重建 |
| 平推法预测 | 基于最新时点利率不变，向前生成 250/500/750/2500 个交易日 |
| 折现率生成 | 基础曲线(750MA→4.5%终极, 20-40年过渡) + 综合溢价(前20年/长期口径+20-40线性插值) → 即期折现率 → 远期折现率 `F_t=((1+S_t)^t/(1+S_{t-1})^(t-1))-1` |
| 对比日期 | 上月末/上年末自动取最后一个有数据交易日 |
| 数据加载 | 5 级降级：内嵌数据 → data.json → /api/data → localStorage → 空状态 |
| 路由 | URL hash（`#gov_spot`/`#premium`/`#preset`），支持刷新/分享直达 |

**配色约定**：涨红跌绿（`#c0392b` / `#27ae60`），符合中国市场习惯 ✅

---

## 六、整体评价

### 优点

1. **业务完整度高**：从基础曲线→溢价→折现率→预定利率，形成完整的寿险精算利率监测链路，口径定义清晰（README + methodology 双文档）。
2. **数据自动化成熟**：GitHub Actions 每日 5 次抓取 + keepalive 防休眠 + 文件大小校验 + 补班日核验，运维设计周到。
3. **性能有考量**：移动平均用前缀和优化，避免每次重算；`life_discount.json` 只存紧凑数据降低传输。
4. **可用性细节**：5 级数据降级、localStorage 离线缓存、Excel 上传/导出、打印样式、响应式断点（1200/900/768/640/380）。
5. **口径严谨**：地方政府债 bootstrap、逆周期溢价修正口径、旧/新准则并排对比，体现专业度。

### 不足（按优化价值排序）

#### 🔴 高优先级

1. **单文件巨石结构（5323 行）**：CSS+HTML+JS 全部内联在 index.html，无模块化、无构建。任何改动都要在超大文件中定位，极易误伤，diff 难审。**建议**：抽离为 `css/style.css` + `js/` 分模块（core/ma/discount/preset/render），引入轻量构建或至少 ES Module。

2. **存在死代码/不可达代码**：
   - L4466 `if (false && thead && tbody)` — 永不执行的折现率表渲染分支。
   - L4500 `return;` 后 `downloadDiscountGenerationExcel` 仍有大段不可达旧逻辑。
   - `downloadLifeDiscountExcel()` 直接 `return;` 空实现。
   - L517 寿险合同负债评估卡 `display:none` 整块保留但未启用。
   **建议**：清理死代码，或明确标注"保留原因"。

3. **节假日/补班日维护分散且易漂移**：`index.html` 的 `CHINA_HOLIDAYS`(至2027) + `TRADE_WEEKENDS` 与 `ci_update.py` 的 `MAKEUP_WORKDAY_CANDIDATES` 是三套并行硬编码，每年需手动同步多处。**建议**：抽成单一 `holidays.json` 数据文件，前端与 CI 共用，避免口径漂移。

#### 🟡 中优先级

4. **assets 本地化导致仓库膨胀**：echarts.min.js、xlsx.full.min.js 全量本地副本，仓库体积大、版本升级困难。**建议**：评估改用 CDN（带 SRI）或仅按需引入 XLSX 子包。

5. **错误处理与加载体验弱**：各板块无独立 loading skeleton / error boundary；图表初始化失败时静默；`fetchFromServer` 失败仅 console.warn。**建议**：加统一错误提示与重试，区分"数据为空"与"加载失败"。

6. **localStorage 缓存陈旧风险**：缓存无 TTL/版本号，数据更新后旧缓存可能误导。**建议**：缓存写入 schema 版本号，过期自动失效。

7. **可访问性（a11y）不足**：大量 `onclick` 内联、卡片可点击但无 `role/tabindex/键盘支持`、图表无文字降级、颜色对比度部分偏低。**建议**：交互卡加 ARIA + 键盘可达。

8. **重复 DOM 操作与全量重渲染**：部分切换会 `renderAll()` 全量重绘，移动平均表格大数据量下可能卡顿。**建议**：按需局部更新，虚拟滚动长表。

#### 🟢 低优先级

9. **无版本号/变更日志**：前端无版本标识，排障时难确认线上版本。**建议**：注入 build hash 或版本号到页脚。

10. **魔法数字散落**：终极利率 4.5%、25BP 触发线、MAX_REASONABLE_RATE=8 等硬编码于代码中。**建议**：集中到配置常量区并注释口径来源。

11. **打印样式可增强**：三大板块切换式 tab 在打印时只能打当前页；折现率左右对比在打印窄幅下可能错位。

12. **测试覆盖**：`tests/` 存在但前端逻辑（移动平均、折现率公式）缺端到端断言。**建议**：对核心算法补单元测试。

---

## 七、后续改动建议（待你确认后再动手）

1. 先做**结构调整**（拆分文件）还是先做**功能增强**？
2. 死代码清理是否可立即执行（需逐处确认保留意图）？
3. 节假日数据是否统一为 JSON 共享文件？
4. 隐藏的"寿险合同负债评估"卡是计划启用还是删除？

> 我已完整掌握代码层与功能层，随时可按你的优先级开始精准改动。
